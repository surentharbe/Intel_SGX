enclave {
    trusted {
        /* define ECALLs here. */
		public void TrustedFn([out, size=len] char* buf, size_t len);    
    };

    untrusted {
        /* define OCALLs here. */
    
    };
};
